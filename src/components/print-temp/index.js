
import printTemp from './index.vue';

/* istanbul ignore next */
printTemp.install = function (Vue) {
    Vue.component(printTemp.name, printTemp);
    Vue.component(printTemp.name, printTemp);
};

export default printTemp;
