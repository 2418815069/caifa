import xcyList from './index.vue';

/* istanbul ignore next */
xcyList.install = function (Vue) {
    Vue.component(xcyList.name, xcyList);
    Vue.component(xcyList.name, xcyList);
};

export default xcyList;
